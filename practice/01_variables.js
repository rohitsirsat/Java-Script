/* variables why not var
because var creates an issue in block scope and functional scope
*/
const num = 123;
let firstName = "rohit";
var lastName = "sisat";
cityName = "Akola"; // js allow it but prefer not to use
let moNumber;

console.table([num, firstName, lastName, cityName, moNumber]);
